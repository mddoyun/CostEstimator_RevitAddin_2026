<Window x:Class="RevitDjangoConnector.ConnectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitDjangoConnector"
        mc:Ignorable="d"
        Title="Revit-Django Connector" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Server URL:" VerticalAlignment="Center"/>
            <TextBox x:Name="ServerUrlTextBox" Width="200" Text="ws://127.0.0.1:8000/ws/revit-connector/" VerticalAlignment="Center" Margin="5,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="ConnectButton" Content="Connect" Width="100" Margin="5" Click="ConnectButton_Click"/>
            <Button x:Name="DisconnectButton" Content="Disconnect" Width="100" Margin="5" IsEnabled="False" Click="DisconnectButton_Click"/>
        </StackPanel>
        <Grid Grid.Row="2" Margin="0,5,0,0">
            <ProgressBar x:Name="ProgressBar" Height="20"/>
            <TextBlock x:Name="ProgressTextBlock" Text="Ready" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
        </Grid>
        <TextBox x:Name="StatusTextBox" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                 Text="Ready to connect."/>
    </Grid>
</Window>